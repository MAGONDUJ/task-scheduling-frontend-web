import * as React from 'react';
import { IPropsFieldBase } from '../interfaces/props';
declare const useValidation: ({ name, value, validation, validationContext, validationAttributeNames, errorMessage: errorProp, children }: IPropsFieldBase<{}>) => {
    isValid: boolean;
    errorMessage: string;
    showError: boolean;
    dirty: boolean;
    setDirty: React.Dispatch<React.SetStateAction<boolean>>;
    submitted: boolean;
};
export default useValidation;
