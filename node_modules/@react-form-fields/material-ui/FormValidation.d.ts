import * as React from 'react';
interface IProps extends React.Props<IFormValidationRef> {
    onSubmit: (valid: boolean) => void;
    className?: string;
}
export interface IFormValidationRef {
    isValid(formSubmitted?: boolean): Promise<boolean>;
    reset(): void;
}
declare const FormValidation: React.MemoExoticComponent<React.ForwardRefExoticComponent<Pick<IProps, "onSubmit" | "className" | "key" | "children"> & React.RefAttributes<IFormValidationRef>>>;
export default FormValidation;
